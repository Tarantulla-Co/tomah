import Image from 'next/image';import type {Metadata} from 'next';import {notFound} from 'next/navigation';import {api,ApiError} from '@/lib/api';const site=process.env.TOMAH_PUBLIC_SITE_URL||'https://tomah-international.tarantulla-co.chatgpt.site';export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{try{const r=await api.getRecipe((await params).slug);return{title:`${r.title} | Tomah International`,description:r.excerpt,alternates:{canonical:`${site}/recipes/${r.slug}`},openGraph:{title:r.title,description:r.excerpt,images:[new URL(r.image.url,site).toString()]}}}catch{return{}}}export default async function Page({params}:{params:Promise<{slug:string}>}){let r;try{r=await api.getRecipe((await params).slug)}catch(e){if(e instanceof ApiError&&e.status===404)notFound();throw e}return <main id="main" className="store-main"><div className="store-detail"><Image src={r.image.url} alt={r.image.alt} width={900} height={700}/><article><p className="store-kicker">Tomah recipe</p><h1>{r.title}</h1><p className="store-intro">{r.excerpt}</p><h2>Ingredients</h2><ul>{r.ingredients.map(x=><li key={x}>{x}</li>)}</ul><h2>Method</h2><ol>{r.instructions.map(x=><li key={x}>{x}</li>)}</ol></article></div></main>}
