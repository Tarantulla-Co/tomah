import Image from 'next/image';import Link from 'next/link';import type {Metadata} from 'next';import {api} from '@/lib/api';export const metadata:Metadata={title:'Recipes | Tomah International',description:'Simple serving ideas featuring pure Tomah maple products.'};export default async function Page(){const{items}=await api.getRecipes();return <main id="main" className="store-main"><p className="store-kicker">Serving inspiration</p><h1 className="store-title">A little maple goes a long way.</h1><div className="store-grid" style={{marginTop:38}}>{items.map(r=><article className="store-card" key={r.id}><Link href={`/recipes/${r.slug}`}><Image src={r.image.url} alt={r.image.alt} width={700} height={500}/></Link><div className="store-card-body"><h2><Link href={`/recipes/${r.slug}`}>{r.title}</Link></h2><p>{r.excerpt}</p></div></article>)}</div></main>}
