import type {Metadata} from 'next';import {TrackingLookup} from '@/components/storefront/track-order';export const metadata:Metadata={title:'Order status | Tomah International',robots:{index:false,follow:false}};export default async function Page({params,searchParams}:{params:Promise<{orderNumber:string}>;searchParams:Promise<{email?:string}>}){const{orderNumber}=await params,{email=''}=await searchParams;return <main id="main" className="store-main"><p className="store-kicker">Order status</p><h1 className="store-title">{orderNumber}</h1><TrackingLookup initialNumber={orderNumber} initialEmail={email}/></main>}
