import type {Metadata} from 'next';import {api} from '@/lib/api';export const metadata:Metadata={title:'Frequently asked questions | Tomah International'};export default async function Page(){const{items}=await api.getFaqs();return <main id="main" className="store-main"><p className="store-kicker">Help</p><h1 className="store-title">Frequently asked questions.</h1><div className="store-form">{items.map(item=><details key={item.id} style={{borderTop:'1px solid #d4d8dd',padding:'20px 0'}}><summary style={{fontWeight:700,cursor:'pointer'}}>{item.question}</summary><p className="store-intro">{item.answer}</p></details>)}</div></main>}
