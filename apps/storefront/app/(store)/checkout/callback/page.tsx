import type {Metadata} from 'next';import {TrackingLookup} from '@/components/storefront/track-order';export const metadata:Metadata={title:'Confirming payment | Tomah International',robots:{index:false,follow:false}};export default async function Page({searchParams}:{searchParams:Promise<{payment_intent?:string;redirect_status?:string;orderNumber?:string;email?:string}>}){const p=await searchParams;return <main id="main" className="store-main"><p className="store-kicker">Payment return</p><h1 className="store-title">Confirming your order.</h1><div className="store-notice">Returning here does not itself confirm payment. Tomah's server confirms it via Stripe's webhook — this page refreshes automatically until it does.</div><TrackingLookup initialNumber={p.orderNumber||''} initialEmail={p.email||''} auto/></main>}
