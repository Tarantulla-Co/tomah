import type {Metadata} from 'next';import {TrackingLookup} from '@/components/storefront/track-order';export const metadata:Metadata={title:'Confirming payment | Tomah International',robots:{index:false,follow:false}};export default async function Page({searchParams}:{searchParams:Promise<{reference?:string;orderNumber?:string;email?:string}>}){const p=await searchParams;return <main id="main" className="store-main"><p className="store-kicker">Payment return</p><h1 className="store-title">Confirming your order.</h1><div className="store-notice">Returning from Paystack does not itself confirm payment. Refresh below while Tomah’s server receives the payment confirmation.</div><TrackingLookup initialNumber={p.orderNumber||''} initialEmail={p.email||''} auto/></main>}
