import Image from 'next/image';import Link from 'next/link';import type {CatalogueResponse,Category} from '@/lib/api/types';import {formatMoney} from '@/lib/money';
export const categoryLabels:Record<Category,string>={POULTRY:'Poultry',PORK:'Pork',MEATS:'Meats',SEAFOOD:'Seafood',GRAINS:'Grains',VEGETABLES_AND_FRIES:'Vegetables & Fries',MAPLE_PRODUCTS:'Maple Products'};
export function Catalogue({data,active,query='',sort=''}:{data:CatalogueResponse;active?:Category;query?:string;sort?:string}){const base=active?`/categories/${active}`:'/products',href=(page:number)=>`${base}?${new URLSearchParams({...(query?{q:query}:{}),...(sort?{sort}:{}),page:String(page)})}`;return <><form className="store-tools"><input className="store-input" name="q" defaultValue={query} placeholder="Search products" aria-label="Search products"/><select className="store-select" name="sort" defaultValue={sort} aria-label="Sort products"><option value="">Featured</option><option value="name">Name</option><option value="price-asc">Price: low to high</option></select></form><nav className="store-categories" aria-label="Product categories"><Link className={`store-chip ${!active?'active':''}`} href="/products">All</Link>{Object.entries(categoryLabels).map(([key,label])=><Link key={key} className={`store-chip ${active===key?'active':''}`} href={`/categories/${key}`}>{label} ({data.categoryCounts[key as Category]})</Link>)}</nav>{data.items.length?<><div className="store-grid">{data.items.map(p=><article className="store-card" key={p.id}><Link href={`/products/${p.slug}`}><Image src={p.image.url} alt={p.image.alt} width={700} height={700}/></Link><div className="store-card-body"><p className="store-kicker">{categoryLabels[p.category]}</p><h2><Link href={`/products/${p.slug}`}>{p.name}</Link></h2><p>{p.shortDescription}</p><div className="store-card-bottom"><span>{p.category==='MAPLE_PRODUCTS'?`From ${formatMoney(p.priceFrom,p.currency)}`:'Individual quote'}</span><Link href={p.category==='MAPLE_PRODUCTS'?`/products/${p.slug}`:'/quote'}>{p.category==='MAPLE_PRODUCTS'?'View product':'Request quote'} →</Link></div></div></article>)}</div>{data.total>data.pageSize&&<nav className="store-pagination" aria-label="Catalogue pages">{data.page>1&&<Link className="store-button secondary" href={href(data.page-1)}>Previous</Link>}<span>Page {data.page} of {Math.ceil(data.total/data.pageSize)}</span>{data.page*data.pageSize<data.total&&<Link className="store-button secondary" href={href(data.page+1)}>Next</Link>}</nav>}</>:<div className="store-empty"><h2>No products found</h2><p>Try another search or browse all categories.</p><Link className="store-button" href="/products">View all products</Link></div>}</>}
