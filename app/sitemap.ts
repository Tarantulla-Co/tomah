import type {MetadataRoute} from 'next';import {api} from '@/lib/api';export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.TOMAH_PUBLIC_SITE_URL||'https://tomah-international.tarantulla-co.chatgpt.site';const[{items:products},{items:recipes}]=await Promise.all([api.listProducts({pageSize:24}),api.getRecipes()]);const staticRoutes=['','/products','/wholesale','/wholesale/apply','/quote','/recipes','/faq','/about','/contact','/legal/privacy','/legal/terms','/legal/returns'];return[...staticRoutes.map(url=>({url:base+url,lastModified:new Date()})),...products.map(p=>({url:`${base}/products/${p.slug}`,lastModified:new Date()})),...recipes.map(r=>({url:`${base}/recipes/${r.slug}`,lastModified:new Date()}))]}
